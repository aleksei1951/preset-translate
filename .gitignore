node_modules/

# runtime config & state
.translaterc.json
glossary.json
*.log

# preset JSON files (user content, not part of the tool)
*.json
!package.json
!package-lock.json

# dev artifacts (tests, prototypes, notes)
dev/
